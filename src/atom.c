#include "atom.h"

#include <assert.h>
#include <stddef.h>

const char *atom_string(const char *str)
{
    assert(str);

    return NULL;
}

int atom_length(const char *str)
{
    assert(str);

    return 0;
}
